apiVersion: v1
kind: Service
metadata:
  name: meal-appointment
spec:
  selector:
    app: meal-appointment
  ports:
    - name: api
      protocol: TCP
      port: 4000
      targetPort: api
    - name: web
      protocol: TCP
      port: 80
      targetPort: web
